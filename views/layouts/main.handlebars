<!DOCTYPE html>
<html lang="en">

{{!-- main handlebars is the skeleton of the website, will show on every page --}}

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VetTech</title>
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
       <link rel="stylesheet" href="/css/style.css" />
</head>

<body style="background-color: rgb(76,72,72);">
    <!--header-->
    {{#if loggedIn}}
    <header class="header">
        <!--header title -->
        <img class="nav-logo" src="/images/VETLOGO.svg" alt="vet-tech-text-logo">
        <div class="nav-div">
        <a href="/api/petdashboard" class="navbar-text">Pets</a>
        <a href="/api/ownerdashboard" class="navbar-text">Owners</a>
                <a href="/api/user/logout" class="navbar-text" id="logout">Logout</a>
                </div>
        <!-- search button -->
        <button class="search-button">Search</button>
        <!-- search input -->
        <input class="search-input" type="text" placeholder="Search Pets or Owners..">
    </header>
    {{/if}}

    <div>
        {{!-- renders contents of sub layout --}}
        {{{ body }}}
    </div>

    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
    <script src="/js/bootstrap.js"></script>
</body>

</html>