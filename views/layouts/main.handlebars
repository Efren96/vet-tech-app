<!DOCTYPE html>
<html lang="en">

{{!-- main handlebars is the skeleton of the website, will show on every page --}}

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VetTech</title>
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Crimson+Text:ital,wght@0,400;0,600;0,700;1,400;1,600;1,700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">

<body>
    <!--header-->
    {{#if loggedIn}}
    <header class="header">
        <!--header title -->
        <a href="/api/petdashboard"><img class="nav-logo" src="/images/VETLOGO.svg" alt="vet-tech-text-logo"></a>
        <div class="nav-div">
            <a href="/api/petdashboard" class="navbar-text">Pets</a>
            <a href="/api/ownerdashboard" class="navbar-text">Owners</a>
            <a href="/api/user/logout" class="navbar-text" id="logout">Logout</a>
        </div>
        <!-- search button -->
        <button class="search-button">Search</button>
        <!-- search input -->
        <input class="search-input" type="text" placeholder="Search Pets or Owners..">
    </header>
    {{/if}}

    <div class="card-section-div">
        {{!-- renders contents of sub layout --}}
        {{{ body }}}
    </div>

    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
    <script src="/js/bootstrap.js"></script>
    <script src="/js/search-input.js"></script>
</body>

</html>